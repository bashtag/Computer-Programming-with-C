int i, j;
	// char	temp[100];
	
	// // bubble sort loop
	// for (i = 0; i < n; i++)
	// {
	// 	for (j = i + 1; j < n; j++)
	// 	{
	// 		// if arr[i] > arr[j], I swap these strings
	// 		if (strcmp(arr[i], arr[j]) > 0)
	// 		{
	// 			strcpy(temp, arr[i]);
	// 			strcpy(arr[i], arr[j]);
	// 			strcpy(arr[j], temp);
	// 		}
	// 	}
	// }